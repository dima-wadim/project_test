# setup.py

from setuptools import setup, find_packages

setup(
    name='geometry',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Укажите зависимости здесь
    ],
    test_suite='tests',
)
